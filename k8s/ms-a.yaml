apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-a
  namespace: llkhe
  labels:
    app: ms-a
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-a
  template:
    metadata:
      labels:
        app: ms-a
    spec:
      containers:
      - name: ms-a
        image: ms-a:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8181
        volumeMounts:
        - name: ms-a-config
          mountPath: /opt/apache-karaf/etc/person-ms-a.cfg
          subPath: person-ms-a.cfg

        readinessProbe:
          tcpSocket:
            port: 8181
          initialDelaySeconds: 40
          periodSeconds: 10

        livenessProbe:
          tcpSocket:
            port: 8181
          initialDelaySeconds: 60
          periodSeconds: 20
      volumes:
      - name: ms-a-config
        configMap:
          name: ms-a-config
---
apiVersion: v1
kind: Service
metadata:
  name: ms-a
  namespace: llkhe
spec:
  selector:
    app: ms-a
  ports:
  - port: 8181
    targetPort: 8181
    nodePort: 30181
  type: NodePort
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ms-a-config
  namespace: llkhe
data:
  person-ms-a.cfg: |
    # Kafka
    kafka.bootstrap=kafka:9092
    kafka.topic=person-events

    # Redis
    redis.host=redis
    redis.port=6379
